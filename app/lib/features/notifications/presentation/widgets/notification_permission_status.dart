import 'package:flutter/material.dart';

class NotificationPermissionStatus extends StatelessWidget {
  const NotificationPermissionStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
