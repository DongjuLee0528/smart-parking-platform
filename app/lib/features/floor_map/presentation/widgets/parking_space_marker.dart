import 'package:flutter/material.dart';

class ParkingSpaceMarker extends StatelessWidget {
  const ParkingSpaceMarker({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
