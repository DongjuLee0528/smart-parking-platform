import 'package:flutter/material.dart';

class ParkingLotDetailPage extends StatelessWidget {
  const ParkingLotDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
