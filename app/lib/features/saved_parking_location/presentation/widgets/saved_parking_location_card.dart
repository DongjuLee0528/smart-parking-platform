import 'package:flutter/material.dart';

class SavedParkingLocationCard extends StatelessWidget {
  const SavedParkingLocationCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
