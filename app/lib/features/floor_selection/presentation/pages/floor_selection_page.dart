import 'package:flutter/material.dart';

class FloorSelectionPage extends StatelessWidget {
  const FloorSelectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
